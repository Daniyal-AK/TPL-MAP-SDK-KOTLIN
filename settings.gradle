pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter()
        maven {
            url "https://api1.tplmaps.com/artifactory/example-repo-local/"
            allowInsecureProtocol = true
        }
    }
}
rootProject.name = "TPL MAP SDK SAMPLE"
include ':app'
